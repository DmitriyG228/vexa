You are working in a meeting-notes workspace. The directory `notes/` contains one markdown
note per past meeting.

Task: answer the questions listed in `questions.md`. Write your answers to `answers.md` in
this exact format — for each question:

```
## <question id>
<answer in 1-3 sentences>
Source: <relative path of the note file the answer comes from>
```

Rules:
- Every answer MUST cite the note file it came from on its `Source:` line.
- Answer only from the notes in this workspace. If the notes do not contain the answer,
  write "not found in workspace".
- Do not ask the user anything; complete the task autonomously and then stop.
