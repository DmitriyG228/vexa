You are working in an operations workspace.

Task, in order:
1. Read `data/services.txt`.
2. Find every service marked `DOWN`.
3. Write `ops/restart-plan.md` containing an H1 line `# Restart plan` followed by a
   markdown bullet list of the DOWN service names in alphabetical order, one per bullet.
4. Write `ops/ack.txt` containing exactly one line: `ACK <n>` where `<n>` is the number of
   DOWN services.

Create the `ops/` directory if needed. Do not modify `data/services.txt`. Do not ask the
user anything; complete the task autonomously and then stop.
