# golden vectors — proposal.v1

Committed example vectors. Each `<Shape>.<case>.json` validates against `#/$defs/<Shape>` via `../validate.mjs`. The goldens ARE the spec (P8): a pending L2 comment, an approved L3 open_pr (per-action decision stamped), and a batch L2 Decision.
