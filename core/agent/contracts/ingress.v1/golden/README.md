# golden vectors — ingress.v1

Committed example vectors. Each `<Shape>.<case>.json` validates against `#/$defs/<Shape>` via `../validate.mjs`. The goldens ARE the spec (P8): one issue delivery and one PR delivery, each carrying an opaque `github://` ref and a payload digest — never the payload bytes.
