# deployment — deploy-target pages (Mintlify)

Per-target deployment guides: `lite.mdx` (the one-container Vexa Lite, process runtime backend)
and `helm.mdx` (the Kubernetes chart, bots as Pods). The Docker Compose path — the default —
lives one level up in [`../deployment.mdx`](../deployment.mdx); nav order is in `../docs.json`.
