# clients — client-surface pages (Mintlify)

User-facing client documentation: `terminal.mdx` (the browser workbench, the primary client).
The API clients (curl/SDK) are covered by the API reference; nav order is in `../docs.json`.
