# api — OpenAPI surfaces (Mintlify pages)

Consumer-facing API references: `agent.mdx` (the agent-api control plane — invocations, chat SSE,
sessions, routines), `meetings.mdx` (the meetings/bot control plane), `mcp.mdx` (the MCP server
fronting the public API for AI clients), and `errors.mdx` (the shared error reference).
